"""jobfinder — a tiny, profile-driven, rule-based job aggregator.

Generic infrastructure (scrape / classify / report / web). The search itself —
what roles to surface and the location lens — is defined in profile.yaml, and
the companies to scrape in companies.yaml. See README.md."""

__version__ = "0.1.0"
